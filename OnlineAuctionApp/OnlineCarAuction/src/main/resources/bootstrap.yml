security:
  basic:
    enabled: false
    
management:
  security:
    enabled: false

spring:
  cloud:
    config:
        uri: http://localhost:8888
  application:
    name: tradeRevConfigServer   
  profiles:
      active: dev 